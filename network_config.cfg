network:
  version: 1
  config:
  - type: physical
    name: eth0
    subnets:
      - type: static
        address: ${ip_host}/${network_size}
        gateway: ${ip_gateway}
  - type: nameserver
    address:
      - ${dns}
